decribe('/login', () => {
  beforeEach(() => {
    cy.visit('/#/login');
  });

  if (('great with Sign in', () => {})) it('links to #/register', () => {});

  it('reguires email', () => {});
  it('requires password', () => {});
  it('requires valid username and password', () => {});

  it('navigates to #/ on successful login', () => {});
});
